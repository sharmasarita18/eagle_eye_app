pipeline {
    agent none
    stages {
         stage("git check out") {

             agent any

             steps {
                git branch: 'developer',
                credentialsId: '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',
                url: 'https://github.com/sharmasarita18/eagle_eye_app.git'
            }

         }
        stage('Build image') {
            agent {
        core_svc/dockerfile true
    }
            steps {
                echo 'Hello world!' 
                sh 'echo myname = $myname'
            }
        }
    }
}