pipeline {
    agent none
    stages {
        stage("Fix the permission issue") {

            agent any

            steps {
                sh "sudo chown root:jenkins /run/docker.sock"
                sh "sudo chown root:jenkins /run/docker.sock"
            }

        }
        stage('Stage 1') {
            agent {
        dockerfile true
    }
            steps {
                echo 'Hello world!' 
                sh 'echo myname = $myname'
            }
        }
    }
}